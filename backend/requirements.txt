# Core API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiohttp>=3.9.0
aiofiles>=23.2.1
websockets>=12.0
Pillow>=10.0.0

# ComfyUI client (already included via aiohttp)

# FireRed / Qwen image editing
# NOTE: transformers must be pinned at 4.57.6 for FireRed compatibility
transformers==4.57.6
torch
torchvision
accelerate
peft
sentencepiece
huggingface_hub
numpy

# FireRed needs bleeding-edge diffusers — install from git:
# pip install git+https://github.com/huggingface/diffusers.git
# The line below is a fallback; prefer the git install in the Kaggle notebook
diffusers>=0.32.0
